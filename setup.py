from setuptools import setup, find_packages

setup(
    name = "protobuf-json",
    version = "0.1",
    url = 'http://github.com/stanhu/protobuf-json',
    license = 'BSD',
    description = "JSON serialization support for Google's protobuf Messages",
    author = 'Paul Dovbush',
    packages = ['protobuf_json']
)

